{% extends 'base.html.twig' %}

  {% block menu %}
            <ul class="nav nav-treeview">
              <li class="nav-item">
                <a href="{{path('provider_list')}}" class="nav-link">
                  <i class="far fa-circle nav-icon"></i>
                  <p>Provider List</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="{{path('provider_new')}}" class="nav-link">
                  <i class="far fa-circle nav-icon"></i>
                  <p>Provider Add</p>
                </a>
              </li>
            </ul>

            <ul class="nav nav-treeview">
              <li class="nav-item">
                <a href="{{path('sendmail')}}" class="nav-link">
                  <i class="far fa-circle nav-icon"></i>
                  <p>Ecrire un message</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="{{path('cours')}}" class="nav-link active">
                  <i class="far fa-circle nav-icon"></i>
                  <p>Conversion Devise</p>
                </a>
              </li>
            </ul>
            {% endblock %}
{% block body %}

<!-- left column -->
          <div class="col-md-12">
            <!-- general form elements -->
            <div class="card card-primary">
              <div class="card-header">
                <h3 class="card-title">Faire une conversion de devise</h3>
              </div>
              <!-- /.card-header -->
              <!-- form start -->
              <form method="POST" action="">

              <label for="devisecible">Conversion de la source : USD</label>
                <div class="card-body">
                  <div class="form-group">
                    <label for="devisecible">Devise cible</label>
                    <select  class="form-control" name="devisecible" id="devisecible">
                      <option value="-" disabled selected>-</option>
                      <option value="TND">TND</option>
                      <option value="EUR">EUR</option>
                    </select>
                
                  </div>

                  <div class="form-group">
                    <label for="montant">Montant</label>
                    <input type="number" name="montant" class="form-control" id="montant" placeholder="Enter le sujet de l'email">
                  </div>
                  <div class="form-group">
                    <label for="message">Resultat</label>
                    <textarea name="resulat" class="form-control" id="message" placeholder="résultat de conversion">{{resultat}}</textarea>
                  </div>      
                </div>
                <!-- /.card-body -->

                <div class="card-footer">
                  <button type="submit" class="btn btn-primary">Convertir</button>
                </div>
              </form>
            </div>
            <!-- /.card -->

           
          

                
              
                <!-- /input-group -->
              </div>
              <!-- /.card-body -->
            </div>
            <!-- /.card -->
            <!-- Horizontal Form -->
        
            <!-- /.card -->

          </div>
          <!--/.col (left) -->
  {% endblock %}