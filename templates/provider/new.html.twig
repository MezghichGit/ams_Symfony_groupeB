{% extends 'base.html.twig' %}



{% block body %}
    <h1>Create new Provider</h1>

    {{ include('provider/_form.html.twig') }}

    <a class="btn btn-warning" href="{{ path('provider_list') }}">back to list</a>
{% endblock %}
